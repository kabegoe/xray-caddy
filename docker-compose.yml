version: '3.8'
services:

  xray:
    build:
      context: ./xray/docker
      args:
        - version=v1.3.0
        - timezone=Asia/Tokyo
    network_mode: "host"
    volumes:
      - ./xray/config.json:/etc/xray/config.json
      - caddy_data:/data
    restart: unless-stopped

  caddy:
    build:
      context: ./caddy/docker
      args:
        - version=2.3.0
    network_mode: "host"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - SITE_ADDRESS=<your domain:8443>
      - CLOUDFLARE_API_TOKEN=<API token from Cloudflare>
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
